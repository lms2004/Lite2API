services:
  cliproxyapi:
    profiles: ["oauth"]
    build:
      context: ./third_party/cliproxyapi
      dockerfile: Dockerfile
      args:
        VERSION: v6.10.9
        COMMIT: 785b00c3127eea6aa207f1207ead8a2aa93690a3
        BUILD_DATE: fixed-source
    image: lite2api/cliproxyapi:v6.10.9
    container_name: lite2api-cliproxyapi
    restart: unless-stopped
    network_mode: host
    user: "10001:10001"
    read_only: true
    cap_drop: ["ALL"]
    security_opt:
      - no-new-privileges:true
    cpus: 0.75
    mem_limit: 256m
    pids_limit: 128
    tmpfs:
      - /tmp:size=16m,noexec,nosuid
    volumes:
      - ./channels/runtime/cliproxyapi/config.yaml:/CLIProxyAPI/config.yaml:ro
      - ./channels/runtime/cliproxyapi/auths:/run/cliproxyapi/auths
    healthcheck:
      test: ["CMD", "wget", "-q", "-T", "5", "-O", "/dev/null", "http://127.0.0.1:45682/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  grok2api:
    profiles: ["grok"]
    image: "${GROK2API_IMAGE:-ghcr.io/chenyme/grok2api:latest}"
    container_name: lite2api-grok2api
    restart: unless-stopped
    network_mode: host
    init: true
    stop_grace_period: 30s
    command: ["/app/grok2api", "--config", "/app/config.yaml", "--listen", "127.0.0.1:45680"]
    security_opt:
      - no-new-privileges:true
    volumes:
      - ./channels/runtime/grok2api/config.yaml:/run/grok2api/config.yaml:ro
      - lite2api-grok-data:/app/data
      - lite2api-grok-quality:/var/lib/grok2api-quality-guard
    healthcheck:
      test: ["CMD", "wget", "-q", "-T", "5", "-O", "/dev/null", "http://127.0.0.1:45680/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  gemini-web2api:
    profiles: ["gemini"]
    build:
      context: .
      dockerfile: channels/gemini-web2api/Dockerfile
    image: lite2api/gemini-web2api:local
    container_name: lite2api-gemini-web2api
    restart: unless-stopped
    network_mode: host
    user: "10001:10001"
    read_only: true
    cap_drop: ["ALL"]
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=32m,noexec,nosuid
    volumes:
      - ./channels/runtime/gemini-web2api/config.json:/app/config.json:ro
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:45681/', timeout=5).read()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  lite2api-grok-data:
  lite2api-grok-quality:
